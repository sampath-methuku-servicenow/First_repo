pipeline {
    agent any
stages {

             
         stage('Build-Step') {
 
             steps {
                     echo 'Build Step FisrtRepo '
             }
         }
     

        stage('Change-Step') {
              steps {
                   echo 'Change Step'
                   
              }
        }
}
   
}
